package main

import "fmt"

type Node struct {
	data   int
	height int
	right  *Node
	left   *Node
}

type AvlTree struct {
	root *Node
}

func (tree *AvlTree) insert(root *Node, data int) *Node {
	if(tree.root == nil)
}

func main() {

}
